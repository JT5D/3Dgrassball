- name: setup-awscli
  # You may pin to the exact commit or the version.
  # uses: hsupu/setup-awscli@beaae6cf06e493a4f929e4e2967d79ca9c73eb7b
  uses: hsupu/setup-awscli@v1.0.0
  with:
    # 
    install-dir: # optional, default is /usr/local/aws-cli
    # 
    bin-dir: # optional, default is /usr/local/bin
    # 
    gpg-verify: # default is true
